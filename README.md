# Class assignments

Private repo for CMPE 148 coding assignments. Each task lives in its own folder (for example `hw2-task4/`).

## Workflow

1. Add or edit files for the assignment.
2. Commit:
   ```bash
   git add .
   git commit -m "Add assignment-name"
   git push
   ```
