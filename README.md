# Class assignments

Private repo for coding assignments. Each task lives in its own folder (for example `task4/`).

## Workflow

1. Add or edit files for the assignment.
2. Commit:
   ```bash
   git add .
   git commit -m "Add task N"
   git push
   ```
